Run unit tests with `cargo test` or run a single test with `cargo test <test-name>`.

Unit tests should follow the form <scenario>_<expectation>.

When finalizing files, makes sure that `cargo test` passes and use `cargo fmt` to format all files.